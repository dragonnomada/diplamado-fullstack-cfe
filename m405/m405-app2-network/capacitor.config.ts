import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'm405-app2-network',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
