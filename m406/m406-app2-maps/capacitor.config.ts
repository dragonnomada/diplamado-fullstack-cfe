import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'm406-app2-maps',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
